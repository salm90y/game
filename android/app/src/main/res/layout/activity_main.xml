<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000">

    <!-- Intentionally a plain Android view at startup. The native PS1 surface is
         created only after a ROM is successfully selected. -->
    <View
        android:id="@+id/startup_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#000000"
        android:importantForAccessibility="no" />

    <ImageButton
        android:id="@+id/btn_discrete_settings"
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:layout_gravity="top|end"
        android:layout_margin="12dp"
        android:background="@drawable/bg_round_discrete"
        android:contentDescription="@string/action_settings"
        android:src="@android:drawable/ic_menu_manage"
        android:alpha="0.45" />

</FrameLayout>
