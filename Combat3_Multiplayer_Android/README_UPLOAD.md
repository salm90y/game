# Combat3 Multiplayer Android — isolated project

This directory is reserved for the repaired Android project from `Combat3_Multiplayer_Android_Repaired(1).zip`.

The project must remain isolated from the existing `android/` project in this repository.

Expected build entry point: `settings.gradle.kts` / `build.gradle.kts` with the Android module under `app/`.
